import StubPage from "../components/StubPage";

export default function AutomationsPage() {
  return (
    <StubPage
      title="Automations"
      description="The rule list/editor lands in FE-4. This route is wired up and ready for that ticket to fill in."
    />
  );
}
