import StubPage from "../components/StubPage";

/** Route shell only — the device grid itself is FE-2's scope, not this ticket's. */
export default function DevicesPage() {
  return (
    <StubPage
      title="Devices"
      description="The device grid lands in FE-2. This route is wired up and ready for that ticket to fill in."
    />
  );
}
