import StubPage from "../components/StubPage";

export default function LogsPage() {
  return (
    <StubPage
      title="Logs"
      description="Per-device log viewing is part of FE-3's device detail page. This route is a placeholder until a dedicated cross-device logs view is scoped."
    />
  );
}
