import StubPage from "../components/StubPage";

export default function AgentsPage() {
  return (
    <StubPage
      title="Agents"
      description="The read-only agents/audit view lands in FE-5. This route is wired up and ready for that ticket to fill in."
    />
  );
}
